para compilar el proyecto en una terminal debemos posicionarnos en la ruta en la que 
se encuentra el pom padre y ejecutar:

$<directorio_de_instalacion_de_maven>/bin/mvn compile

y a continuación para empaquetar en un jar (ejecutando los test):

$<directorio_de_instalacion_de_maven>/bin/mvn package